/** Modelo de una imagen obtenida desde el servicio de Spotify. */
export default interface ImageItem {
    /** Altura de la imagen. */
    height: number,
    /** Direccion de la imagen. */
    url: string,
    /** Ancho de la imagen */
    width: number,
}