import {StyleSheet} from 'react-native';
import {BLACK_1} from '../../../styles/Colors';

const MyLibraryStyles = StyleSheet.create({
    mainContainer: {
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: BLACK_1,
        width: '100%',
        height: '100%'
    }
})

export default MyLibraryStyles;